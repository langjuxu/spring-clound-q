spring:
  application:
    name: zipkin-server
server:
  port: 9411
management:
  metrics:
    web:
      server:
        auto-time-requests: false
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9110/eureka/
